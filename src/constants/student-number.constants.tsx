export const grade = Array.from({ length: 3 }, (_, i) => ({
  value: i + 1,
}));

export const class_no = Array.from({ length: 4 }, (_, i) => ({
  value: i + 1,
}));

export const student_no = Array.from({ length: 20 }, (_, i) => ({
  value: i + 1,
}));