export const studentGrade = Array.from({ length: 3 }, (_, i) => ({ value: String(i + 1) }))
export const studentClass = Array.from({ length: 4 }, (_, i) => ({ value: String(i + 1) }))
export const studentId = Array.from({ length: 18 }, (_, i) => ({ value: String(i + 1) }));